## TODO

### Context
- The original `data/unique_questions.csv` was generated by running `aily-super-agent/scripts/data/export_unique_questions.py`.

### Done
- Created `normalize_questions.py`.
- Filtered the dataset to `chat_origin == "free_question"`.
- Built `effective_question` using `override_question` when present, otherwise `question`.
- Normalized question text.
- Removed empty and very short questions.
- Saved the cleaned dataset to `data/unique_questions_normalized.csv`.
- Created `embed_questions.py`.
- Generated reduced embeddings from the normalized dataset.
- Saved the reduced embedding dataset to `data/unique_questions_reduced_embeddings.csv`.

### Next
- Create a clustering script using HDBSCAN on the embedded questions.
- Compare clustering settings and outputs without regenerating embeddings.
- Save cluster assignments in a new dataset version.

### After Clustering
- Sample representative questions from each cluster.
- Use an LLM to summarize what each cluster is about.
- Propose a human-readable label for each cluster / Let the LLM do it.
- Review whether clusters should be merged, split, or discarded as noise.

### Later
- Turn clusters into a draft taxonomy.
- Decide whether the taxonomy is stable enough for manual annotation.
- Evaluate whether a classifier is feasible.
